# Strongbox Opening prefabs

Create the finished screen prefab here:

```text
StrongboxScreen.prefab
```

The screen prefab may reference common prefabs from `Prefabs/UI/Common`, but it must not
contain reward generation, persistence or inventory authority.
