# Strongbox Opening screen art

Place artwork used only by the Strongbox Opening screen here.

First optional file:

```text
strongbox_opening_bg_ai.png
```

The strongbox itself belongs in `Art/Content/Strongboxes`. Shared reward frames,
buttons and currency icons belong in `Art/UI/Common`.
