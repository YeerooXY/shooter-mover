# Stage 1 production presentation

This directory owns the retained Stage 1 Unity presentation runtime. The first extraction preserves the accepted visible-slice behavior and serialized script GUID while removing scene ownership from the TestSupport folder. Follow-up splits are constrained to presentation responsibilities and must not create new gameplay authorities.
